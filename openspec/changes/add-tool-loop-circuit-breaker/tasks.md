## 1. 熔断核心模型

- [x] 1.1 定义共享的规范工具策略目录、动作族默认值、逐工具覆盖、调用记录、观察快照、警告/阻断结果和稳定原因代码等类型，并校验阈值关系与滑动窗口容量。
- [x] 1.2 实现工具名和参数的递归稳定化，忽略展示字段并统一受支持的工具别名。
- [x] 1.3 实现 nodeId 优先、相近坐标量化、滑动方向/区域/幅度分桶和等待时长分桶等工具族归一化规则。
- [x] 1.4 实现固定容量滑动窗口、执行前阻断检查、执行后记录、非连续等价动作查找和警告桶节流。
- [x] 1.5 实现按规范工具名解析“动作族默认值 + 稀疏用户覆盖”的有效策略，确保别名复用规范工具配置、未知新工具获得默认值、安全豁免不可覆盖。

## 2. 设置存储与工具页

- [x] 2.1 扩展 `Settings` 和默认设置，增加逐工具稀疏覆盖表并升级设置版本；对嵌套对象做显式规范化，非法项逐工具回退且不影响其他设置。
- [x] 2.2 为设置存储增加单工具更新、单工具恢复默认和全部工具恢复默认的安全更新路径，避免浅合并覆盖其他工具配置。
- [x] 2.3 在设置页新增独立“工具”页签，使用共享策略目录按类别展示全部规范工具及当前预设启用状态。
- [x] 2.4 为每个受管工具增加独立警告/阻断步进器、有效范围提示和恢复默认操作；安全豁免工具显示说明但禁用阻断配置。
- [x] 2.5 让阈值编辑始终维持 `warning < block <= historySize`，设置立即持久化但明确提示仅对下一次新任务生效。

## 3. UI 进展判定

- [x] 3.1 定义可从现有 AgentLoop 状态生成的观察指纹，覆盖规范化无障碍树、前台包名/窗口和可用的截图新鲜度信号。
- [x] 3.2 过滤屏幕序列化结果中的已知易变字段，避免时钟、动画进度和瞬态计数单独造成虚假进展。
- [x] 3.3 实现工具结果、前后观察和 TodoList 状态的组合进展判定，并确保工具返回成功但 UI 不变时仍可记录为无进展。
- [x] 3.4 验证相同滚动在页面持续变化时重置无进展序列、触底后开始累计无进展。

## 4. AgentLoop 集成

- [x] 4.1 在每次任务开始时从当前设置生成不可变逐工具策略快照，在结束时清理任务级熔断状态，并删除旧的 `lastToolKey`/连续重复计数逻辑。
- [x] 4.2 在受管工具执行前调用熔断检查；阻断时跳过底层 handler 并产生兼容现有协议的 `LOOP_BLOCKED` 工具失败结果。
- [x] 4.3 在每个动作完成现有稳定等待后记录工具结果与前后观察，确保批量工具调用仍按动作顺序获得正确状态。
- [x] 4.4 为用户决策、技能读取、TodoList、任务完成和任务失败配置豁免/只读语义，保证终止与确认路径始终可用。
- [x] 4.5 将一次性恢复警告注入下一轮最新用户消息，包含动作摘要、次数和适用恢复路径，并避免每轮重复。

## 5. Prompt 与可观测性

- [x] 5.1 在历史格式化中把同一等价动作的重复无进展记录折叠为一条摘要，同时保留最近一次具体错误与 hint。
- [x] 5.2 确认历史折叠不会移除原始任务、用户修正、TodoList、已加载技能约束和最新屏幕观察。
- [x] 5.3 将 warning、blocked、recovered 事件接入现有任务日志，只记录工具族、不可逆短指纹、计数和原因代码。
- [x] 5.4 增加日志隐私保护，确保完整屏幕树、截图、用户输入、验证码和敏感节点内容不会写入熔断事件。

## 6. 自动化验证

- [x] 6.1 增加设置存储和策略解析测试，覆盖默认值、逐工具隔离、非法项回退、嵌套迁移、新工具默认值、别名和安全豁免。
- [x] 6.2 新增 AgentLoop 行为测试，验证相同失败点击在阈值前允许执行、达到配置阈值后只警告一次、最终在执行前阻断。
- [x] 6.3 增加轻微坐标漂移、参数键顺序和展示字段变化的等价动作场景，并验证明显不同目标不会共享计数。
- [x] 6.4 增加正常滚动持续变化不熔断、列表触底后警告并阻断、输入与等待使用各自阈值的场景。
- [x] 6.5 增加只读/豁免工具、不同工具穿插、阻断后换路恢复、新任务重置、任务策略快照和循环历史折叠场景。
- [ ] 6.6 若现有组件测试环境可用，增加工具页的编辑、约束联动、单项/全部恢复测试；否则将这些交互纳入 ADB 验收。
- [x] 6.7 运行熔断相关定向 Jest 测试、完整 `npm test` 和 TypeScript typecheck，记录并仅修复与本变更相关的失败。

## 6A. 工具可用性与元信息配置

- [x] 6A.1 定义并持久化逐工具 `enabled`、显示名称和模型描述稀疏覆盖，完成版本迁移、规范名归一化、长度校验及安全关键工具保护。
- [x] 6A.2 在工具页增加独立启用开关、显示名称和模型描述编辑，并让单项/全部恢复默认同时清理阈值和元信息覆盖。
- [x] 6A.3 在 AgentLoop/AgentToolkit 创建时应用工具配置快照：过滤禁用工具、覆盖描述，并在执行边界以 `TOOL_DISABLED` 拒绝禁用工具。
- [x] 6A.4 增加设置存储与 AgentToolkit 测试，覆盖逐工具隔离、预设覆盖、描述替换、恢复默认、安全工具和禁用 handler 不执行。
- [x] 6A.5 运行定向 Jest、完整测试与 TypeScript typecheck，并构建 Android APK。
- [x] 6A.6 参考 OpenMinis 将 `browser_use` 默认描述改为触发场景、推荐动作流程、能力边界与安全约束导向，并增加模型描述契约测试。

## 7. Android 构建与 ADB 真机验收

- [x] 7.1 构建可安装的 Android APK，使用 `adb devices` 确认目标设备在线并安装当前构建。
- [ ] 7.2 确认真机已启用豆泡无障碍服务、悬浮窗和测试所需截图权限，并使用无敏感数据的测试页面。
- [ ] 7.3 打开“设置 > 工具”，验证全部规范工具、预设禁用状态、安全豁免状态、阈值联动以及单项/全部恢复默认。
- [ ] 7.4 为 `tap` 保存较低阈值，重启应用并开始新任务；验证设置持久化、按配置警告/阻断，且 `scroll` 阈值未被改变。
- [ ] 7.5 执行稳定无效的重复点击场景，通过行为与过滤 logcat 验证警告、阻断阈值以及被阻断动作未下发。
- [ ] 7.6 执行长列表正常滚动再触底的场景，验证内容变化时不误报、触底无进展时最终阻断。
- [ ] 7.7 验证运行中修改阈值不改变当前任务快照；停止并启动新任务后新阈值生效且旧熔断历史已清空。
- [ ] 7.8 验证收到 `LOOP_BLOCKED` 后模型可通过返回、重新打开应用或改用节点/坐标操作继续任务。
- [ ] 7.9 检查日志不包含输入文本、截图或完整页面内容；根据真机结果校准量化和易变字段规则后重新运行自动化测试。
