## 1. 规格同步

- [x] 1.1 将设计修订为 L2 单遍固定规则卸载、唯一 LLM 摘要阈值和自然语言摘要
- [x] 1.2 明确 `ContextCompressionManager` 单类内聚边界和旧滑动窗口回滚要求

## 2. 上下文管理器

- [x] 2.1 新增上下文轮次、检查点、准备结果和类型化错误
- [x] 2.2 实现模型窗口解析、保守 Token 估算和唯一摘要阈值
- [x] 2.3 实现幂等的 L2 单遍固定规则卸载并保护最近 4 个完整轮次
- [x] 2.4 实现中文自然语言摘要提示词、无工具调用、临时错误重试和 abort 防提交
- [x] 2.5 实现滚动检查点、`<context_summary>` 注入和摘要后单次重新估算

## 3. AgentLoop 接入

- [x] 3.1 为历史轮次增加稳定 ID
- [x] 3.2 将请求前上下文处理接入 `ContextCompressionManager.prepare()` 单一入口
- [x] 3.3 保留旧 `maxHistoryItems` 路径，并由配置开关选择新旧策略
- [x] 3.4 保证静态 system prompt、工具协议和完整 AgentEvent 不变

## 4. 配置与界面

- [x] 4.1 新增默认开启的 `contextCompressionEnabled` 设置和兼容迁移
- [x] 4.2 设置页增加“智能上下文压缩”开关；关闭时显示旧滑动窗口配置
- [x] 4.3 将开关和模型标识传入普通 AgentLoop 与 TaskPlanner

## 5. 测试与验证

- [x] 5.1 添加 L2 单遍、最近轮次保护、幂等性和工具配对测试
- [x] 5.2 添加低于阈值不摘要、达到阈值只摘要一次、自然语言注入和超限终止测试
- [x] 5.3 添加开关默认值、设置迁移和旧滑动窗口回滚测试
- [x] 5.4 运行 TypeScript 类型检查及相关 Jest 测试
- [x] 5.5 构建 Android release APK；本次未要求装机，真机验证留待后续

## 6. 摘要边界（已由第 7 节修订）

- [x] 6.1 完成第一版全量历史摘要边界（后续改为只摘要较早前缀）
- [x] 6.2 完成第一版检查点后增量注入（后续恢复最近轮次原文保护）
- [x] 6.3 保持 Todo 为摘要之外唯一独立的实时任务状态
- [x] 6.4 补充摘要替代、Todo 独立更新和旧滑动窗口兼容测试
- [x] 6.5 运行类型检查、相关 Jest 测试及 OpenSpec 校验

## 7. 最近对话保护与缓存口径

- [x] 7.1 恢复最近 4 个完整模型轮次的摘要外保护，仅摘要较早历史前缀
- [x] 7.2 将运行中补充输入从特殊通道改为普通 user 消息语义
- [x] 7.3 保持 Todo 独立，并验证摘要、最近轮次和新用户消息的组合结构
- [x] 7.4 将缓存命中率分母修正为完整 prompt tokens 并补充测试
- [x] 7.5 运行类型检查、相关 Jest 测试及 OpenSpec 校验

## 8. 瞬态 UI 观察

- [x] 8.1 将 UI 结构观察拆分为完整原始事件、稳定短历史结果和下一决策瞬态载荷
- [x] 8.2 将瞬态结构放在最新缓存断点之后，并在成功决策后清除、推理重试时复用
- [x] 8.3 移除上下文管理器对历史完整 UI 观察的额外保护
- [x] 8.4 添加单次发送、重试复用、原始事件保留和缓存前缀稳定测试
- [x] 8.5 运行 TypeScript 类型检查、相关 Jest 测试及 OpenSpec 校验
