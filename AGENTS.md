# 工程协作约定

## Spec 文档语言

- `specs/`、`openspec/` 及其他需求、方案、任务拆解类 Spec 文档，默认优先使用中文描述。
- 与 Spec 配套的 Plan、Tasks 默认使用中文编写。
- 代码标识符、API 字段、类型名、枚举值、文件名、命令和协议名称保留英文原文，避免与实现契约不一致。
- 必须引用英文标准或原文时，可保留英文，并在必要时补充中文说明。
